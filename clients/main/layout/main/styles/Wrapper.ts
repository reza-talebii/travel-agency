import { Layout } from 'antd'
import styled from 'styled-components'

export const MainLayoutStyled = styled(Layout)`
  .content {
    background-color: #fafbfc;
    min-height: calc(100vh - 580px);
    height: 100%;
    padding: 40px 104px;
  }
`
