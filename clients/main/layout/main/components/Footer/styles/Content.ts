import { Row } from 'antd'
import styled from 'styled-components'

export const ContentFooterWrapper = styled(Row)`
  align-items: flex-end;
  justify-content: flex-end;
  padding: 42px 104px 64px;
  gap: 64px;
  width: 1440px;
  height: 422px;
  background: #8dd3bb;
`
